diff --git a/dist/index.cjs b/dist/index.cjs
index eaaca64c8cea62cae05fa5b88a338e0edbe6629d..429a3698463608709e2bcf7b4f69d12c797fcb61 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -64,11 +64,10 @@ function __awaiter(thisArg, _arguments, P, generator) {
 let otelModulePromise = null;
 const OTEL_PKG = "@opentelemetry/api";
 function loadOtel() {
-	if (otelModulePromise === null) otelModulePromise = import(
-		/* webpackIgnore: true */
-		/* @vite-ignore */
-		OTEL_PKG
-).catch(() => null);
+	// Patched: dynamic import() removed — Hermes (React Native) cannot compile it.
+	// IronLog does not use @opentelemetry/api; tracing stays disabled. See patches/.
+	if (otelModulePromise === null) otelModulePromise = Promise.resolve(null);
+	void OTEL_PKG;
 	return otelModulePromise;
 }
 /**
diff --git a/dist/index.mjs b/dist/index.mjs
index a34e2816ac9b0e83dd28346a76da8232d7d02aa8..59c231a2c967156d2497551d3804cbf6648dc75f 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -68,11 +68,10 @@ function __awaiter(thisArg, _arguments, P, generator) {
 let otelModulePromise = null;
 const OTEL_PKG = "@opentelemetry/api";
 function loadOtel() {
-	if (otelModulePromise === null) otelModulePromise = import(
-		/* webpackIgnore: true */
-		/* @vite-ignore */
-		OTEL_PKG
-).catch(() => null);
+	// Patched: dynamic import() removed — Hermes (React Native) cannot compile it.
+	// IronLog does not use @opentelemetry/api; tracing stays disabled. See patches/.
+	if (otelModulePromise === null) otelModulePromise = Promise.resolve(null);
+	void OTEL_PKG;
 	return otelModulePromise;
 }
 /**
