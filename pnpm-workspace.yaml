packages:
  - .

# Require packages to sit in the registry before install, reducing exposure to
# compromised releases that are usually removed quickly.
minimumReleaseAge: 7200

allowBuilds:
  dtrace-provider: true
  esbuild: true
  unrs-resolver: true
