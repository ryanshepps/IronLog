packages:
  - .

# pnpm 11 defaults to a 24-hour minimum release age. Renovate PRs already
# commit reviewed frozen lockfiles, so keep CI installs deterministic.
minimumReleaseAge: 0

allowBuilds:
  dtrace-provider: true
  esbuild: true
  unrs-resolver: true
